{
  "name": "votewise",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "db:push": "DATABASE_URL=\"file:./server/prisma/dev.db\" prisma db push --accept-data-loss --schema=./server/prisma/schema.prisma",
    "db:seed": "DATABASE_URL=\"file:./server/prisma/dev.db\" node server/prisma/seed.js",
    "build": "DATABASE_URL=\"file:./server/prisma/dev.db\" prisma db push --accept-data-loss --schema=./server/prisma/schema.prisma && DATABASE_URL=\"file:./server/prisma/dev.db\" node server/prisma/seed.js && npm run build --workspace=client"
  },
  "dependencies": {
    "@prisma/client": "^6.19.2"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prisma": "^6.19.2"
  }
}
